---
// CardDescription.astro
import { cn } from "../../lib/utils"

interface Props {
  class?: string
}

const { class: className, ...props } = Astro.props
---

<div
  data-slot="card-description"
  class={cn("text-muted-foreground text-sm", "lg:text-lg mb-3 lg:mb-6", className)}
  {...props}
>
  <slot />
</div>
