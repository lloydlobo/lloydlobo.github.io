import "@testing-library/jest-dom/vitest"

// Import test helpers to make them available globally
// import "./helpers/render"
// import "./helpers/mocks"

// Optional: make commonly used helpers global
// (global as any).renderAstroComponent = (await import("./helpers/render")).renderAstroComponent
